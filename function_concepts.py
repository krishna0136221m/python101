#first  we imported maths_function.py file to use function which is written in that file
from maths_function import add, subtract, multiply, power, divide, number

print(add(2, 3))
print(subtract(2, 1))
print(divide(6, 2))
print(multiply(2, 2))
print(power(2, 3))

print(number)
